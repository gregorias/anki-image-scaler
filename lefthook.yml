pre-commit:
  scripts:
    "format-files.bash":
      runner: bash
    "check-asset-version.py":
      runner: python3
  commands:
    pipenv-check:
      run: "pipenv check"
    unit-tests:
      run: "./dev/bin/testall"
commit-msg:
  commands:
    commitlint:
      run: "commitlint --edit={1}"
